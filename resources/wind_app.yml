# Wind Turbine Analytics App Configuration

resources:
  apps:
    wind_turbine_app:
      name: "wind-analytics-${var.env}"
      description: "Système d'analyse SCADA pour parcs éoliens - ${var.env}"

      # Source code path (required for Databricks Apps)
      source_code_path: .
